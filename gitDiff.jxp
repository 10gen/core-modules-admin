<% if(! request.file) return; %>
<%
core.content.html();
core.git.repo();
var g = new git.Repo();
var diff = g.diff(request.getParameters("file"), {rev: "HEAD"});
adminRoot.pieces.gitDiff({output: diff.out});

%>
